<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="nct.servicerequest.mapper.SvcReqImageMapper">

    <!-- 요청서 이미지 전체 삭제 — 수정 시 재삽입 전 호출 -->
    <delete id="deleteBySvcReqSn">
        /* SvcReqImageMapper.deleteBySvcReqSn */
        DELETE FROM SVC_REQ_IMAGE WHERE SVC_REQ_SN = #{svcReqSn}
    </delete>

    <!-- 요청서 이미지 목록 조회 — FILES JOIN으로 URL 반환. 정렬순 -->
    <select id="findImagesBySvcReqSn" resultType="nct.servicerequest.dto.SvcReqImageItem">
        /* SvcReqImageMapper.findImagesBySvcReqSn */
        SELECT
            i.FL_SN  AS flSn,
            f.FL_PATH AS url
        FROM SVC_REQ_IMAGE i
        JOIN FILES f ON f.FL_SN = i.FL_SN
        WHERE i.SVC_REQ_SN = #{svcReqSn}
        ORDER BY i.SVC_REQ_IMG_SORT_NO ASC
    </select>

    <!-- 요청서 이미지 목록 일괄 추가 -->
    <insert id="insertAll">
        /* SvcReqImageMapper.insertAll */
        INSERT INTO SVC_REQ_IMAGE (SVC_REQ_SN, FL_SN, SVC_REQ_IMG_SORT_NO)
        VALUES
        <foreach collection="images" item="img" separator=",">
            (#{img.svcReqSn}, #{img.flSn}, #{img.svcReqImgSortNo})
        </foreach>
    </insert>

</mapper>
