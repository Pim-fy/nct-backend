<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="nct.product.mapper.ProductTradeRegionMapper">

    <!-- 희망 거래지역 전체 삭제 — 수정 시 재삽입 전 호출 -->
    <delete id="deleteByPrdSn">
        /* ProductTradeRegionMapper.deleteByPrdSn */
        DELETE FROM PRODUCT_TRADE_REGION WHERE PRD_SN = #{prdSn}
    </delete>

    <!-- 희망 거래지역 목록 조회 -->
    <select id="findByPrdSn" resultType="nct.product.dto.ProductTradeRegionItem">
        /* ProductTradeRegionMapper.findByPrdSn */
        SELECT
            RGN_CD AS code,
            RGN_NM AS name
        FROM PRODUCT_TRADE_REGION
        WHERE PRD_SN = #{prdSn}
        ORDER BY PRD_TRD_RGN_SN ASC
    </select>

    <!-- 희망 거래지역 목록 일괄 추가 -->
    <insert id="insertAll">
        /* ProductTradeRegionMapper.insertAll */
        INSERT INTO PRODUCT_TRADE_REGION (PRD_SN, RGN_CD, RGN_NM)
        VALUES
        <foreach collection="regions" item="rgn" separator=",">
            (#{rgn.prdSn}, #{rgn.rgnCd}, #{rgn.rgnNm})
        </foreach>
    </insert>

</mapper>
